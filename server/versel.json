{
  "buildCommand": "cd client && npm istall && vite build",
  "version": 2,
  "builds": [
   
    {
      "src": "server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
